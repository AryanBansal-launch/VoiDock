services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - '8080:8080'
      - '80:80'
    networks:
      - voidock-network

networks:
  voidock-network:
    name: voidock-network
    driver: bridge